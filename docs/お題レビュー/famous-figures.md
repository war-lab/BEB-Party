# お題レビュー: Famous Figures（famous_figures_v1）

## 結論

200枚・制約カード6枚・重要表現9件を収録した。検証12項目とリントをすべて通過している。

**レビューの水準はカードによって違う**。
`card_001`〜`card_036` は3観点のクロスレビューを2巡経ている。
あとから追加した `card_037`〜`card_200` の164枚は、2026-09-07に**3観点のクロスレビュー1巡**を経た（1巡少なく、衝突の裁定は裁定者1人が行った）。

このレビューで**過去に確定した判断の回帰が1件見つかった**。詳細は後述の「回帰の修正」に記す。
以下の「クロスレビュー（AI・3観点）」の節は最初の36枚についての記録であり、164枚の記録は「クロスレビュー1巡（2026-09-07）」の節に分けて置く。

人手レビューはAIによる3観点のクロスレビューを2巡行った。
1巡目で各観点が独立に評価し、2巡目で互いの判断が衝突した6点を裁定した。
その結果、カード13枚の禁止語・正解を差し替え、制約カード2枚の英文を直し、重要表現を4件更新した。

**実プレイは経ていない**。知名度・所要時間・得点分布は未実測であり、後述の未解決の論点として残す。

お題データの正本は `content/dontsayit/famous-figures.json`、ルールの正本は [基本設計/09](../基本設計/09_DONTSAYITゲームモジュール.md) である。

## 経緯

| 日付 | 出来事 |
| --- | --- |
| 2026-08-18 | 4観点の敵対検証で、机上で判定できる欠陥8枚を差し替え（正解の隣の固有名詞が未禁止、Messi と Ronaldo が相互に区別できない等） |
| 2026-08-19 | 3観点のクロスレビュー1巡目。各観点が独立に36枚を評価 |
| 2026-08-19 | 同2巡目。判断が衝突した6点を裁定し、カード13枚・制約カード2枚・重要表現4件を確定 |
| 2026-08-19 | PRレビューの指摘により、山札の下限の導出（最大消費枚数）を訂正 |
| 2026-09-07 | 64枚を追加して100枚とした。検証9項目を通過。クロスレビューは未実施 |
| 2026-09-07 | 100枚を追加して200枚とした。知識寄りの層は [Great Figures](./great-figures.md) へ分離。日本人名の表記規約を適用し `Nobunaga Oda` → `Oda Nobunaga`、`Ryoma Sakamoto` → `Sakamoto Ryoma` へ変更 |
| 2026-09-07 | 3観点のクロスレビュー1巡。回帰2枚を差し替え、禁止語4枚を修正、7枚へ別名を付与。検証10〜12を通過 |
| 2026-09-08 | 禁止語を5語から15語へ。2,000語を追加し、検証が20件の誤りを落とした。記録済みの穴4枚を塞いだ |

## セットの骨格

| 項目 | 内容 |
| --- | --- |
| カード | 200枚（検証4の下限30枚に対して170枚の余裕。下限の導出は[基本設計/09](../基本設計/09_DONTSAYITゲームモジュール.md)） |
| 制約カード | 6枚（`verbs_only` / `three_words` / `no_country` / `question_form` / `past_tense` / `no_job`） |
| 重要表現 | 9件 |
| 正解の語数 | 1語89枚 / 2語102枚 / 3語8枚 / 4語1枚 |
| 別名を持つカード | 7枚（`Santa Claus` / `Detective Conan` / `Goku` / `Superman` / `Batman` / `Spider-Man` / `Hulk`） |
| 実在と架空 | 実在の人物69枚 / 架空・キャラクター130枚 / 実在の動物1枚（`Hachiko`） |
| 女性の正解 | 37枚 |

架空が6割を占めるのは、世代と関心が割れても共通基盤が最も太い領域だからである。
分野の偏りは意図したものとして残す。

実在と架空、女性の枚数はデータに分類を持たせておらず、作成時に手で数えた値である（機械で再現できない）。
36枚の時点にあった「分野」と「非人間の正解」の内訳は、200枚では手作業の集計が信頼できないため落とした。

### クロスレビュー1巡（2026-09-07）: 回帰の修正

観点A（知名度）が、**過去のレビューで削除が確定したカードが別idで復活している**ことを指摘した。
これはこの巡で唯一、推定ではなく既存記録との突き合わせで確認できた事実である。

| 復活していたカード | 元の削除理由（本記録の「確定した変更」） | 対処 |
| --- | --- | --- |
| `card_072` Cristiano Ronaldo | `card_023` として「Messi と判別材料が全部禁止語で、禁止語を除くと相互に区別できなかった」ため削除し `Usain Bolt` へ差し替え済み | `Shigeru Miyamoto`（宮本茂）へ差し替え |
| `card_075` Serena Williams | `card_024` として「知名度不足（名前を口頭で産出できない層がいる）」ため削除し `Naomi Osaka` へ差し替え済み | `Yayoi Kusama`（草間彌生）へ差し替え |

復活した2枚は、削除理由となった欠陥をそのまま持っていた。
`Cristiano Ronaldo` は `card_022` Messi と同一セットに共存しており、卓が「サッカー選手」まで到達してから絞れない経路が再発していた（観点Cも独立に同じ指摘をした）。

**原因は、164枚を追加するときに本記録の「確定した変更」を参照しなかったことである。**
機械検証はセット内の正解の重複しか見ないため、「一度削除したカードの復活」は検出できない。
差し替え先を非スポーツにしたことで、スポーツ枠は18枚から16枚（テニス4・サッカー3）へ落ちた。

**再発防止**: カードを追加するときは、そのセットの記録の「確定した変更」を先に読む。
この手順を [README](./README.md) には書いていない。機械で担保する手段が思いつかなかったため、記録として残すにとどめる。

### クロスレビュー1巡（2026-09-07）: 共演キャラクター名の未禁止（46枚）

観点B（説明の成立性）が、このセットの最重要欠陥を1つ立てた。

> **共演キャラクター名を1枚も塞いでいない**（約24枚）。これは1巡目レビューが最重要欠陥として13枚を修正した defect class（`Luigi` / `Nezuko` / `Shinichi` / `Anna` / `Star Wars`）が、追加分でそのまま再発している。難度の設計が5枠のうち実質1〜2枠しか機能していない。

**これは回帰である。** 前節の `Cristiano Ronaldo` / `Serena Williams` と同じく、164枚を追加するときに本記録の「確定した変更」を参照しなかったことが原因である。
機械検証はこの欠陥を検出できない（別のカードの正解を禁止語に書き忘れたことは、文字列比較では判定できない）。

観点Bは「説明が成立しないカードは0枚」と報告しており、問題は一貫して**逆方向**、すなわち5枠で塞ぎきれず1語で即答されるカードの多さである。

修正した46枚の類型を挙げる。

| 類型 | 枚数 | 例 |
| --- | --- | --- |
| 共演・関係者名が未禁止 | 24 | `Hermione Granger` に `Harry` がなく「Harry Potter's friend, the girl」で即答。`Yoshi` / `Princess Peach` / `Bowser` の3枚に `Mario` がない。`Minnie Mouse` / `Donald Duck` / `Goofy` / `Walt Disney` の4枚に `Mickey` がない |
| 相互に名前が未禁止のペア | 7 | `Federer`↔`Nadal`、`Maradona`↔`Pele`、`Jackie Chan`↔`Bruce Lee`、`Buzz Lightyear`↔`Woody`、`Homer`↔`Bart` |
| 代表作名が未禁止 | 2 | `Leonardo DiCaprio` に `Titanic` がない（`ship` は塞ぐが作品名は別語）。`Brad Pitt` は5語すべてが汎用で `card_191` / `card_194` と区別できず、実際の経路（`Angelina` / `Fight Club`）を1枠も塞いでいなかった |
| 歌手8枚が代表曲を1枚も塞いでいない | 8 | `Shake It Off` / `Umbrella` / `Poker Face` / `Single Ladies` はすべて言えた。かつ `singer`+`pop` の2枠が8枚で共通のため、実質3枠しか個体差がなかった。曲名を1位に置き `pop` を落とした |
| 英語の質・死に札 | 2 | `Hatsune Miku` の `twin tails` は**和製英語**（英語は `pigtails`）で、英語話者が言わない語に1枠を使っていた。加えて最強の手がかり `green`（髪の色）が未禁止だった。`Bugs Bunny` の `trickster` は全帯が産出しない死に札 |
| 並び順 | 3 | `Kirby` の `inhale`、`Pablo Picasso` の `cubism` はレベル1〜2が産出しない語であり、3位に置くと空振りする。`Rilakkuma` は語源の `relax` が3位だった |

重要表現も入れ替えた。
観点Bが「164枚の多数がキャラクターで、説明の主経路は外見（`yellow` / `pink` / 髪型 / 服装）だが、外見を述べる型が1件もない」と指摘した。
`He/She wears ...` と `He/She has ... hair.` を追加し、情報量のない `Everyone knows this one.` と、このセットでは使わない `Not a real person.` を落とした。

### クロスレビュー1巡（2026-09-07）: 禁止語の位置に関する規則の新設

観点Cが、既存の並び順の基準（「正解に強く結びつく順」）では検出できない失敗を1つ立てた。

> 提示する語は先頭から取るため、4語目と5語目はレベル1〜2に対して**禁止されない**。つまり機微な語を4〜5位に置くと、言い換えの語彙が最も乏しく、話題の舵を切る余裕が最もない層にだけその語が開く。

`TABOO_COUNT` が `{1:3, 2:3, 3:4, 4:4, 5:5}` であることをコードで確認し、指摘は正しいと裁定した。
その結果、[基本設計/09](../基本設計/09_DONTSAYITゲームモジュール.md) に「並び順の第一基準と、その例外」を新設した（機微な語は強さに関係なく先頭3語へ置く）。

この規則により、このセットで4枚を修正した。

| id | 正解 | 修正前 | 修正後 | 理由 |
| --- | --- | --- | --- | --- |
| `card_196` | Bruce Lee | `kung fu, yellow, fast, actor, shout` | `kung fu, nunchucks, fast, actor, shout` | `yellow` の意図（黄色いトラックスーツ）が語の一覧から復元できず、中国系の人物名の隣に人種語として並ぶ。`debrief` で全員の画面にも出る |
| `card_191` | Tom Cruise | `actor, Hollywood, stunts, short, teeth` | `actor, Hollywood, stunts, Mission, pilot` | `short` と `teeth` が4〜5位にあり、**存命の人物の外見**がレベル1〜2に合法な手がかりとして開いていた |
| `card_099` | Nelson Mandela | `Africa, prison, president, freedom, black` | `Africa, prison, black, president, freedom` | `black` が5位でレベル1〜4に開き、人種語が最短の手がかりになっていた |
| `card_093` | Marilyn Monroe | `actress, blonde, dress, Hollywood, beauty` | `actress, blonde, beauty, dress, Hollywood` | `beauty` が5位に開き、外見と性的イメージが残余経路になっていた |

`card_099` については、`black` を3位に上げるとレベル1〜2が最強の手がかりを1つ多く失う。
観点C自身が「どちらを上位の基準に置くかはこの観点だけでは決められない」と保留した点であり、**裁定として安全性を上に置いた**。難度が上がることは受け入れる。

### 主題を Great Figures と分けた

100枚から200枚へ増やす際、知識寄りの層をこのセットに足さず [Great Figures](./great-figures.md) として分離した。

このセットの成否を分けるのは「卓の関心と世代」である（`Gudetama` を知る層と `Bugs Bunny` を知る層は違う）。
Great Figures の成否を分けるのは「学校で習ったか」である。
1本に混ぜると、レベル1の説明者がマイナーな偉人を引く事故を卓の側で避けられない。

ただしこのセットには36枚の時点から偉人17名（`Albert Einstein` / `Cleopatra` / `Oda Nobunaga` 等）が入っており、**分離は完全ではない**。
既存のカードidを動かさないため移していない。カードidは [既知の課題](../既知の課題.md) から参照されている。

1ゲームで使うのは最大30枚である。
200枚にしたのは1ゲームの都合ではなく、同じ部屋で続けて遊ぶときの再登場を薄めるためである（開示済みのカードは次のゲームの山札に戻る。[既知の課題](../既知の課題.md)）。
36枚では2ゲーム目でほぼ確実に既出のカードが出るのに対し、200枚では出現率が下がる。ただし**再登場そのものは解消していない**。

## 機械検証の結果

`pnpm validate:content` が検証12項目とリントを通過する（エラー0件・警告0件。200枚での実行は2026-09-07）。

機械が見ない項目は次の3つであり、本レビューがその確認にあたる。

* 参加者がその人物を知っていて、名前を口頭で言えるか
* 禁止語が「正解に強く結びつく順」に並んでいるか
* 禁止語を提示したときに説明が成立するか

## クロスレビュー（AI・3観点）

### レビュアーと担当

実施したのはAIエージェント3体である（モデル: fable）。
[基本設計/09](../基本設計/09_DONTSAYITゲームモジュール.md)が「機械で検査しないもの」として人手確認を求めている項目を、実プレイの前段としてAIが代行した記録である。
実際の参加者による確認は未実施であり、後述の「レビューの限界」に残す。

| レビュアー | 担当 |
| --- | --- |
| A | 知名度と参加者適合 |
| B | 説明の成立性 |
| C | 英語と口頭運用 |

3人とも36枚すべてに目を通し、問題なしと判断したカードも明示した。

### 1巡目で立った評価基準

Aが評価の前提を1つ立てた。
このゲームは回答者が**名前を口頭で言えること（想起）**を要求するため、「見れば分かる」知名度では足りない。
名前が出てこない参加者が1人いるとそのカードは死に、スキップは1ラウンド1回しかないため、想起困難なカードが2枚重なるとラウンドが崩れる。

この基準は2巡目でも維持し、知名度に関する判断はすべてこれに従った。

### 2巡目で裁定した衝突

#### 並び順の基準（BとCの衝突）

Bは「正解に強く結びつく順」を、Cは「レベル1〜2が読めない語を先頭に置いても禁止として機能しない」を主張した。

2巡目でBが上位の基準を立て、衝突は解消した。

> 各位置に、その位置を提示されるレベル帯が**実際に使い得る語のうち最強のもの**を置く。

読めない語を先頭に置くコストは「その層への禁止が空振りする」ことだが、その層はその語をそもそも口にできないため、後方へ回しても失うものがない。
一方レベル5では位置に関係なく全語が禁止される。
したがって前方には易しく強い語、後方には難語だが強い語を置く。

Cも同じ結論に達した（「強さとは、その層の話者が実際に使う確率のことだ」）。
以降の並び順はすべてこの基準で確定した。

#### Yoda を差し替えるか（AとBの衝突）

Aは「Star Wars依存が Darth Vader と2枚あり、未視聴者が1人いると2ラウンド影響する」として Dracula への差し替えを提案した。
Bは Yoda を問題なしとし、逆に **Darth Vader の `Star Wars` が未禁止で「the villain of Star Wars」で即割れる**ため Vader 側を要修正とした。
Bの修正を入れると Star Wars を禁止するカードが2枚になり、Aの狙いと逆行する。

**Aが提案を取り下げた。** 知名度の軸で再検証すると、ヨーダもダースベイダーもCM・パロディ経由で作品未視聴層に名前が到達しており、作品知識ではなく名前知識で見れば独立した2枚として扱える。
Aは自分の比較がVaderの穴を見ていなかったことも認めた。

**確定: Yoda 維持、Vader に `Star Wars` を追加。**

#### Serena Williams を直すか差し替えるか（AとBの衝突）

Bは `Venus` が未禁止であることを穴として挙げた。
Aは差し替えを主張し、2巡目でBに反対した。

> 禁止語をいくら直しても知名度不足は治らない。問題は回答者が名前を口頭で産出できないことであり、カード内修正の射程外である。`Venus` の追加はむしろ「直せば残せる」という誤った印象を与える。

**確定: Naomi Osaka へ差し替え。** Aは自分の差し替え案にもBの「関係者名の穴」レンズを適用し、`racket` を落として `US Open` を入れた。

#### Tanjiro を維持するか（AとBの見かけ上の衝突）

Aは「維持」、Bは「`Nezuko` 未禁止で1語で即答されるため要修正」とした。

2巡目でAが、両者は直交していると整理した。
Aの判断は「参加者が知っているか」の軸、Bの指摘は「説明者が抜け道で難度を無効化できるか」の軸である。
Nezuko の追加はむしろAの判断を補強する（開いていると「知名度が高いがゆえに簡単すぎるカード」になる）。

**確定: カード維持＋`Nezuko` を追加。**

#### Elsa の `Frozen`（BとCの衝突）

Cは「日本では『アナ雪』であり英題 Frozen を口にする人はほぼいない。禁止語として死に札」と指摘した。
Bは `Frozen` を1位に残す案を出していた。

**確定: `Anna, ice, snow, queen, Frozen`。** `Frozen` は捨てず5位に置く。英題を口にする可能性があるのはレベル5帯であり、その層にだけ効く（上の統合基準の適用例そのものである）。

#### Detective Conan の枠の取り合い（BとCの衝突）

Bは `Shinichi`（本人の本名。1語で即答）の追加、Cは `bow` の多義解消（`bow tie`）を要求した。枠は5つである。

**両立した。** Cの修正は同一スロット内の表記変更であり、Bの追加と枠を取り合わない。`case` を落として `mystery` と意味領域の重複を解消した。

### 各観点が単独で見つけた主な指摘

| 指摘 | 観点 | 対処 |
| --- | --- | --- |
| `Frozen` と `peanuts` は日本語話者の卓で使われない死に札 | C（Frozen）→ Aが `peanuts` を同型として追加 | 両方差し替え |
| `Luigi` / `Shinichi` / `Nezuko` / `Anna` / `Bonaparte` など、正解の隣にいる固有名詞が塞がれていない | B | 各カードに追加 |
| `plumber` / `mustache` / `reindeer` / `chimney` などがレベル1〜2に読めず、先頭3語で空振りする | C | 並び順を統合基準で確定 |
| Santa Claus は `red` という実際に使われる語が未禁止 | C | `chimney` と差し替え |
| 制約カードの `ja` と `en` が別のルールになっている | C | `verbs_only` と `three_words` の `en` を修正 |
| 重要表現が `He is ...` だけで、女性5枚に使える形がない | A・C | `He/She is ...` へ変更し、3件追加 |
| 制約カードは説明者の秘密にしか載らず、遵守を判定できる役が存在しない | C（1巡目）・B（2巡目で自認） | 制約を公開状態へ移した（設計変更） |
| 禁止語の語形変化（`run`→`runner`、`glove`→`gloves`）の扱いが未定義 | B・C | 遊び方に判定規則を明記 |
| 通称・姓のみ・語順違いの回答をどう扱うかが未定義 | A・C | 遊び方に線引きを明記 |

### レビュアーが自認した見落とし

* A: 禁止語が「日本語話者の卓で実際に使われる語か」を1巡目に検査していなかった。姓名の語順違いを通称問題から漏らした
* B: 並び順を語彙レベルと独立に評価していた。`verbs_only` の根本原因（`ja` と `en` の乖離）を特定したのはCである
* C: 関係者の固有名詞が塞がれているかを36枚に通していなかった。09文書の `WatcherSecret` を巡る自己矛盾に依拠したまま議論していた

## 確定した変更

### カード（13枚）

| id | 変更前 | 変更後 | 理由 |
| --- | --- | --- | --- |
| card_001 Doraemon | `robot, pocket, cat, future, bell` | `robot, cat, Nobita, pocket, future` | 最強の手がかり `Nobita` が未禁止だった |
| card_004 Mario | `plumber, mustache, jump, Nintendo, brother` | `Luigi, Nintendo, jump, brother, plumber` | `Luigi` で1語即答。先頭2語が読めない語だった |
| card_010 Luffy | `pirate, rubber, hat, captain, treasure` | `pirate, One Piece, rubber, hat, captain` | 作品名が未禁止 |
| card_013 Snoopy | `beagle, comic, kennel, peanuts, friend` | `dog, Charlie Brown, comic, house, friend` | `dog` が未禁止。`beagle`/`kennel` は使われない語。`peanuts` は日本で流通しない原作名 |
| card_016 Santa Claus | `reindeer, chimney, present, December, beard` | `Christmas, present, December, red, reindeer` | `Christmas` と `red` が未禁止。先頭2語が読めない語だった |
| card_018 Elsa | `ice, snow, queen, sister, song` | `Anna, ice, snow, queen, Frozen` | `Anna` で1語即答。英題は日本の卓で使われない |
| card_020 Detective Conan | `mystery, child, glasses, bow, case` | `Shinichi, mystery, child, glasses, bow tie` | `Shinichi` で1語即答。`bow` が多義 |
| card_021 Tanjiro | `demon, sword, sister, breathing, earring` | `demon, Nezuko, sword, breathing, sister` | `Nezuko` で1語即答 |
| card_022 Lionel Messi | `soccer, Argentina, goal, small, left` | `soccer, football, Argentina, goal, small` | `soccer` を禁止しても同義語 `football` が残っていた |
| card_023 Cristiano Ronaldo | （カード削除） | **Usain Bolt** `fast, run, Jamaica, gold, record` | Messi と判別材料が全部禁止語で、禁止語を除くと相互に区別できなかった |
| card_024 Serena Williams | （カード削除） | **Naomi Osaka** `tennis, Japan, champion, US Open, serve` | 知名度不足（名前を口頭で産出できない層がいる） |
| card_029 Yoda | `green, small, force, ears, master` | `Star Wars, Jedi, green, small, force` | 作品名が未禁止 |
| card_030 Darth Vader | `mask, black, breathing, father, empire` | `Star Wars, mask, father, black, breathing` | 作品名が未禁止で「the villain of Star Wars」で即割れた |
| card_036 Napoleon | 正解 `Napoleon` | 正解 **`Napoleon Bonaparte`** | `Bonaparte` は正解の構成語ではないため監視役の正解ルールでも止められなかった。正解を2語にすると禁止語を1枠も使わずに塞げる |

### 制約カード

| id | 変更前の `en` | 変更後の `en` | 理由 |
| --- | --- | --- | --- |
| `verbs_only` | `Use verbs, not nouns.` | `Explain mainly with verbs.` | `ja`「動詞を中心に」と別のルールだった。名詞全面禁止と読めるが、名詞ゼロでは "He is a character" すら言えず達成不能 |
| `three_words` | `Three words per sentence.` | `Use three words or fewer per sentence.` | 「ちょうど3語」とも「3語まで」とも読めた |

### 重要表現

* `He is the one who ...` → `He/She is the one who ...`（女性の正解5枚に対応）
* 追加: `It is a character from ...`（36枚中20枚超がキャラクターであり最頻出の型）
* 追加: `Let's skip this one.`（スキップは正規のメカニクスだが、口頭宣言がないと場が止まる）
* 追加: `That's close!`（説明者から回答者へのフィードバック表現が1つもなかった）

### ルールの明文化（遊び方へ追加）

**禁止語の判定**: 複数形・過去形・ing形・「〜する人」は同じ語とみなす（`glove`→`gloves`、`run`→`running`/`runner`、`France`→`French`）。別の単語への言い換えは可（`fast`→`quick`）。監視役は迷ったらセーフにし、ゲームを止めないことを優先する。

**正解の判定**: 日本語読み・姓または名だけ・姓名の語順違い・広く通じる通称は、その人物を一意に指していれば正解とする。作品名や役割だけの回答（「アナ雪」「鬼滅の主人公」）は人物を名指ししていないため正解にしない。

判定基準を「表記一致」ではなく「一意な指示」に置いたのは、説明者の裁量を1点に絞って卓ごとのぶれを減らすためである。

### 設計の変更

制約カードを秘密情報から公開状態へ移した。

レビューで、`WatcherSecret` に制約が含まれず公開状態にもないため、**制約の遵守を判定できる役が存在しない**ことが分かった。
レベル5の制約は事実上の自己申告であり、レベル差の吸収のうちレベル5への上乗せ分が名目だけになっていた。

制約の文言は正解にも禁止語にも触れないため、公開しても秘密は漏れない。

## 禁止語を15語へ増やした（2026-09-08）

収録数を5語から15語へ増やし、レベル別の提示数を 3/5/8/11/15 とした（[基本設計/09](../基本設計/09_DONTSAYITゲームモジュール.md)のレベル差の吸収）。

クロスレビュー観点Bの結論が根拠である。

> 説明が成立しないカードは573枚中0枚。問題は一貫して**逆方向**、すなわち5枠で塞ぎきれず1語で即答されるカードの多さである。

5枠しかないと、有用な禁止語を捨てて別の禁止語を入れる取引が生じていた。
前巡の修正では、共演者名を入れるために既存の禁止語を落とす取引を46回行っている。

### この作業で機械検証が捕まえた誤り

573枚へ10語ずつ足す過程で、検証が**47件**を落とした。

| 検証 | 件数 | 内容 |
| --- | --- | --- |
| 検証2 | 35 | 既存の禁止語と重複する語を書いた |
| 検証10 | 24 | 複合語の構成要素で、規則上すでに禁止されている語を書いた |
| 検証1 | 3 | 正解の構成語を書いた |
| 検証9 | 1 | 3語の禁止語を書いた |
| 検証12 | 1 | 別名と重複する語を書いた |

検証10の24件が最も多い。5語のときは複合語の構成要素を書く余地が少なかったが、10語足すと自然に踏む（`headphones` の `head`、`bookshelf` の `shelf`、`printer` の `print`、`Popeye` の `eye`、`Florence Nightingale` の `night`）。
前巡でこの検査を入れていなければ、規則上すでに禁止されている語で枠を潰したまま気づけなかった。

### 下位の語の質

**6位以降の語は上位より弱い。** 観点Bが「positions 6-15 が死に札の温床になる」と予測したとおりである。

上位5語は「その語を言えば正解が割れる」語だが、下位は「関連はするが説明に使われない語」が混ざる。
実際に説明を塞ぐのは上位8語程度で、レベル4〜5への上乗せは想定より小さい可能性がある。確度: **未検証**。

下位の語の質はカード単位で人手確認していない。実プレイで「その語が実際に出たか」を記録する。

## 未解決の論点

実プレイで確かめる。

| 論点 | 内容 |
| --- | --- |
| 知名度の実測 | 本レビューの知名度判断はすべて推定である。実際に「名前が出てこなかった」カードを記録する。特に Tanjiro（世代）、Naomi Osaka・Usain Bolt（スポーツ関心）、Cleopatra・Napoleon Bonaparte（学校知識の残り方） |
| 追加164枚の知名度 | クロスレビュー1巡を経たが実プレイは経ていない。世代差が特に大きいと見込むのは Popeye・Road Runner・Tweety・Madonna・Elvis Presley（上の世代寄り）と Gudetama・Hatsune Miku・Moana・Ed Sheeran（下の世代寄り）である。観点Cは「5〜6人の卓のどこかで必ず1人は想起できない層のカードが出る」と評価した。確度: **未検証** |
| 観点Aが差し替えを推奨したが維持したカード | `Goldilocks`（「ゴルディロックス」という名前が日本に流通していない）、`Road Runner`（日本での流通が薄い）、`Katniss Everdeen`（作品の到達率が低い）、`Link`（英語の一般語と同綴り）、`Chopper`（同綴りの地雷）。いずれも日本語回答を許す規則と日本語名の表示で緩和されると判断した。**観点Aの評価を否定していない**。実プレイで想起できなかったカードを記録する |
| 同一枠の集中 | 観点Cが実測した内訳はディズニー12枚・Marvel 9枚・ポケモン7枚・任天堂7枚・ギリシャ神話6枚である。1枚目で枠が場に出ると2枚目以降の難度が急落する。特に Marvel は `Groot` / `Deadpool` / `Black Panther` / `Wolverine` が禁止語 `Marvel` を共有し、「枠は分かるが名前が出ない」状態になる |
| 存命の人物の外見が経路になる構造 | 4枚は修正したが、164枚全体に対して「機微な語を4〜5位に置かない」の点検を通していない。観点Cも同じ限界を申告している |
| 記録に留めた抜け道 | 観点Bが挙げた抜け道のうち、次は修正していない。`Spider-Man`（作品軸。ただし別名 `Peter Parker` で一部は塞げている）、`Messi`（`card_022`。1巡目レビュー済みのため今回は触らない）、印象派以外の同一軸重複（`Gudetama` の `lazy`/`tired`、`Iron Man` の `suit`/`armor`）。いずれも成立性は壊しておらず、1語での即答も起きにくいと判断した |
| 制約 `no_country` と `no_job` がキャラクターに効かない | 観点Bの指摘。164枚の多数がキャラクターであり、`Kirby` / `Yoshi` / `Tweety` の説明で国名を言う経路がそもそもない。歌手8枚・俳優8枚は `singer` / `actor` を既に1位に持つため `no_job` も冗長である。基本設計/09が `everyday-things` から `no_country` を外したのと同じ状態がこのセットで起きている。**制約の入れ替えは行っていない** |
| 制約 `past_tense` が存命の人物を誤誘導する | 観点Bの指摘。存命の約40枚を全文過去形で説明すると引退・故人と読める。同セットに `Ichiro Suzuki` と `Michael Jordan` がいるため、`Shohei Ohtani` を過去形で説明すると回答者が Ichiro へ、`LeBron James` を過去形で説明すると Jordan へ誘導される。得点に直結する。確度: **未検証**だが構造的に起きうる |
| ~~追加164枚の禁止語の穴~~（解消） | 15語化で対処した。`Dracula` に `Transylvania`、`Mozart` に `Amadeus` を追加。`Batman` の `bat` は検証10の新設により複合語の構成要素として規則上すでに禁止されている（禁止語欄に書く必要がない） |
| 同一作品からの複数収録 | `Woody` と `Buzz Lightyear`、`Homer Simpson` と `Bart Simpson`、`Kakashi` と `Sasuke` のように、同じ枠から複数のカードが入っている。連続して出ると2枚目が易しくなる可能性がある。山札のシャッフルは主題を見ないため制御していない。確度: **未検証** |
| ポケモン7枚の集中 | `Pikachu` / `Eevee` / `Charizard` / `Jigglypuff` / `Snorlax` / `Meowth` / `Ash Ketchum` が同一セットにある。禁止語に `Pokemon` が共通で入っており、1枚目で「ポケモン」という枠が場に出たあとは2枚目以降が易しくなる |
| 追加100枚の世代差 | `Popeye` / `Road Runner` / `Tweety` / `Madonna` / `Bob Marley` は上の世代寄り、`Hatsune Miku` / `Ed Sheeran` / `Moana` は下の世代寄りである。5〜6人の卓で全員が想起できる保証はない。確度: **未検証** |
| `Link` の正解の広さ | 正解が英語の一般語（link）と同綴りである。説明中に「つながり」の意味で使うと違反になる。`Naomi Osaka` の `Osaka` と同じ性質の地雷である |
| 実在の人物の存命 | `Michael Phelps` / `Oprah Winfrey` / `Jeff Bezos` 等の存命の人物を収録した。外見や振る舞いを説明の材料にすると、卓によって不快になりうる。機械検証の外の論点である |
| Naomi Osaka の地名衝突 | 正解の構成語 `Osaka` は地名と同綴りであり、説明者が出身地の文脈で「Osaka」と言うと違反になる。自然に踏む地雷であり、実プレイで頻度を見る |
| 制約とカードの相性 | `no_country` × Kumamon は地名を使わずに地域性を伝える必要があり難度が高い。Bは「不成立」から「達成困難だが成立する」へ判定を変えたが、実プレイで確かめる |
| Santa Claus の低難度 | 15語化で `sleigh` / `Rudolph` を追加した。レベル1は先頭3語しか提示されないため、初心者向けの易しい札である性質は残る |
| ~~Harry Potter の穴の多さ~~（解消） | 15語化で `Voldemort` / `Hermione` / `muggle` / `magic` をすべて追加した |
| 禁止語の並び順の向き | 「強い順」に並べるとレベル1〜2が最強3語を奪われ、レベル5は弱い2語が足されるだけになる。実効難度が逆転している可能性がある（[既知の課題](../既知の課題.md)、ADR級の判断） |
| 1ラウンドの消費枚数 | 1ラウンド90秒で3〜5枚成立するという見積りが未実測である。山札の下限そのものは消費上限から導いており、この見積りには依存しない |

## レビューの限界

このレビューはAIによるものであり、日本語話者の実在の集団に対する知名度調査ではない。
「20〜30代のテニス無関心層が Serena Williams の名前を言えない」といった判断は、モデルの持つ一般的な知識に基づく推定である。

実プレイで覆る可能性がある判断として、少なくとも次を記録しておく。

* Tanjiro の世代到達度
* Usain Bolt の引退後の想起率
* Charlie Brown（Snoopy の禁止語）が日本語話者の説明で実際に使われるか
